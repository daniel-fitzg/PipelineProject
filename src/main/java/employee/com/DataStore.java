package employee.com;

public class DataStore {

    String storeEmployee() {
        // if DB write is successful return "SUCCESS" or true (If we switch to boolean)
        // else return "FAILURE" or false
        return "";
    }
}
